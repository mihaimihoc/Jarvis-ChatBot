import React from 'react';

const ChatHeader = () => {
  return (
    <header className="chat-header">
      <h1 className="chat-header-title">Jarvis🤖</h1>
    </header>
  );
};

export default ChatHeader;